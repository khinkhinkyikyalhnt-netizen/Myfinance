
import { Transaction } from "../types";

// Feature disabled. Stubbing out service to prevent build errors with process.env
export const generateFinancialInsights = async (transactions: Transaction[]): Promise<string> => {
  return "AI features are currently disabled.";
};
